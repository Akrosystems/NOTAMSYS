# Operational boundary

Single source of truth for what NOTAMSYS actually does versus what still
needs a real-world connection before this can run the Accra International
NOTAM Office. README.md, ARCHITECTURE.md and SECURITY.md link here instead
of each maintaining their own summary, so this table is the one place that
needs updating as capabilities change.

Update this table whenever a capability's status changes. A capability
moving from Stub to Partial or Real is exactly the kind of change that
belongs in a commit message, not just a memory of the work.

| Capability | Status | What's real | What's still needed |
|---|---|---|---|
| Workflow state machine | **Real** | Full received→triage→draft→review→approved→publishing→published lifecycle, enforced transitions, four-eyes control (a preparer cannot approve their own draft) | — |
| Audit trail | **Real** | Append-only `AuditEvent` rows for every transition, evidence attachment, extraction acceptance, rule activation and delivery retry | Export tooling, retention policy |
| Evidence storage | **Real** | SHA-256 content-addressed storage against either the local filesystem (dev) or `MinioStorage` against a real S3-compatible bucket (production Render deployment -- local disk there is ephemeral, so this is the only path evidence actually survives a redeploy) | `MinioStorage`'s bucket connection happens at process startup -- deploying with wrong/missing `NOTAMSYS_OBJECT_STORAGE_*` values crash-loops the API rather than degrading gracefully, so the bucket must exist and be reachable before the first deploy with this backend |
| NOTAM Selection Criteria (Doc 8126 Appendix G) | **Real** (Four-Tier Selection & Disambiguation Engine) | All 13 categories, 1373 rules, with deterministic keyword analysis (`keyword_rules.py`), spatial adjacency disambiguation (e.g. runway strip `MW` vs runway centerline `MR`), synonym-enriched `all-MiniLM-L6-v2` embeddings (`semantic.py`), and officer correction feedback loop (`qcode_corrections` table) | Continuous learning from officer correction feedback |
| Q-line construction rules (Doc 8126 Ch.6) | **Real** | Purpose closed-set enforcement, Item D length cap, referenced-NOTAM existence check, formatter REF-identifier fix, and 24-hour UTC military time input (`UtcDateTimeInput`) | Limit rounding (needs raw-altitude input fields not yet in the schema), FIR multi-code handling |
| AIXM 5.1.1 output | **Real** (event-only profile) | Namespaced Event/EventTimeSlice XML with geodesic circle geometry, generated per NOTAM and viewable in the Published NOTAM detail viewer | Full Digital NOTAM feature modelling (RunwayElement, AirspaceStatus, etc.) — no local spec exists yet to build against (Doc 8126 Part IV is "under development") |
| AFTN/Comsoft publication | **Real** (with manual fallback) | Real envelope construction + ITA-2 character validation (fully testable offline). File-drop, pull-queue bridge (`scripts/aftn_bridge.py`), and manual publish fallback (`mark-published` + per-channel delivery acknowledgment) for operators who manually enter notices in Comsoft/CADAS | Direct bi-directional AFTN socket connection |
| GCAA website / email distribution | **Stub** (GCAA website) / **Partial** (email) | GCAA website: adapter interface exists, simulated success in dev/test. Email: `SmtpEmailAdapter` sends a real message via stdlib `smtplib` once SMTP credentials are configured (`NOTAMSYS_SMTP_*`); falls back to `UnconfiguredAdapter`'s honest failure if the channel is set real without them. Inbound: `app/email_poller.py` reads NOTAM request emails from shared mailbox into the queue as real requests + evidence | Real SMTP/IMAP credentials for aisnotam@caa.gov.gh |
| OCR/NLP document extraction | **Real** (Option A Hybrid Pipeline: PDF & scanned OCR with RapidFuzz Levenshtein typo-correction and local Sentence-Transformers semantic embeddings) | Multi-tier hybrid extraction: deterministic regex parsers for safety-critical values (DTGs, coordinates, limits, ICAO codes), GCAA-AIS-NTM-FR01 form template, RapidFuzz OCR typo correction, expanded ICAO Doc 8400 / EUROCONTROL OPADD Ed 4.1 abbreviation normalizer, and local `all-MiniLM-L6-v2` semantic vector search against Doc 8126 selection criteria rules. `apps/api/Dockerfile` installs `tesseract-ocr`/`tesseract-ocr-eng` and Python extras; `NOTAMSYS_EXTRACTION_ENABLED=true` turns it on | Scanned-image OCR binary execution in production container (proven via `NullOcr` fixtures, unit tests, and PDF text-layer end-to-end tests) |
| Accra FIR AIP reference data | **Real** (Accra FIR 2026 dataset: Ghana AIP 7th Ed + ASECNA eAIP Togo & Benin) | Authentic dataset in `apps/api/app/data/aip/ghana_aip_2026.json` transcribed from `GHANA AIP 7TH ED-21 MAY 26.pdf` and ASECNA eAIP portals (`aim.asecna.aero`) covering the entire Accra FIR (`DGAC`), 17 aerodromes/airstrips across Ghana (`DGAA`, `DGAH`, `DGLE`, `DGLN`, `DGLW`, `DGSI`, `DGSN`, `DGTK`), Togo (`DXXX` Lomé, `DXNG` Niamtougou, `DXAK` Atakpamé, `DXSK` Sokodé), and Benin (`DBBB` Cotonou, `DBBP` Parakou, `DBBN` Natitingou, `DBBK` Kandi, `DBBO` Porga) with real ARP coordinates, elevations, runway designators (`03/21`, `04/22`, `06/24`, etc.), and Navaids (`ACC`, `TML`, `KMS`, `TAD`, `LM`, `LME`, `CBB`, `IACC`, `ITML`). Loaded via `services/aip/` with non-blocking Item A validation | Future automated synchronization with live eAIP XML / AIXM feed when an online API endpoint becomes available |
| Frontend: dashboard, request queue, review, published, quality, rules, integrations, admin | **Real** | All server-rendered from live backend endpoints. Includes dedicated Published NOTAMs Registry (`/published`) with multi-filter search, personnel provenance tracking (preparer + 4-eyes approver), deduplicated channel badges, ICAO Doc 8126 EST/Expiring Originator Outreach template generation, and **Specialist Four-Eyes Verification view** (side-by-side originator request vs prepared NOTAM comparison when reviewing) | Live telemetry stream |
| NOTAM request intake (`/requests/new`, `/submit`) | **Real** | Both the authenticated officer intake and the public originator intake are the exact GCAA-AIS-NTM-FR01 form, field-for-field (Item A location type/value, NOTAM N/R/C with series/no/year reference, Item B/C start/end with 24h UTC `UtcDateTimeInput` supporting `23:00`/`2300`, Item D periods of activity, Item E full text, Item F/G limits, full originator block). Both post to `POST /requests` or `POST /public/requests` | — |
| AIRAC cycle / AIP Supplement cross-reference | **Stub** (AIRAC) / **Real** (AIP Supplement reference) | A NOTAM draft has a real `aip_supplement_reference` field, and saving a PERM NOTAM without one produces a real validation warning (`services/qline.py:validate_perm_aip_supplement`) | Computing "the current AIRAC cycle" itself is deliberately not implemented -- doing so needs a verified historical epoch date this project isn't confident enough in to assert without risking a wrong cycle number stated with false authority. If you need this, verify the epoch against GCAA's or GCAA's published AIRAC schedule before implementing it |
| Authentication & sessions | **Real** | Argon2 password hashing, short-lived JWT access + refresh tokens, httpOnly/Secure/SameSite cookies via the Next.js BFF, deactivated accounts rejected at login (not just on subsequent requests) | MFA, GCAA identity integration, refresh-token rotation/reuse detection |
| RBAC | **Real** | Every mutating endpoint role-gated; a declarative matrix test (`test_rbac_matrix.py`) cross-checks every endpoint × every role in one table, not just per-endpoint assertions | — |
| Superadmin (`SYSTEM_ADMIN`) | **Real** | Universal role-check bypass (`require_roles()`), seeded `admin@notamsys.app` account, user management (`/admin/users` list/create/role/active-status, all audited), can activate rulesets and retry deliveries like a NOF Manager, and can live-edit platform branding (logo/name/subtitle/description) from the admin console -- the one setting on that screen that's genuinely DB-backed and takes effect without a restart, unlike everything else there | No UI for revoking/rotating a user's password from the console (requires the user to be recreated or the DB touched directly) |
| Team Presence | **Real** | Every authenticated user's `last_seen_at` timestamp is updated on each API request (throttled to once per 30 s). `GET /users/presence` returns all active staff with computed Online (≤5 min), Away (≤30 min), or Offline status. A pulsing green **Team Presence** button in the topbar shows the live online count and a drop-down panel with each user's name, role, status badge, and last-seen time. Client sends a background heartbeat every 60 s to maintain online status while the tab is open. | — |

## What "Real" means here

A capability marked **Real** has working code, a test that exercises it
(not just imports it), and no known gap between what it claims to do and
what it does. It does not mean "approved for operational use" — see each
row's "what's still needed" column, and the safety/security stance in
SECURITY.md, for what operational sign-off would actually require.
